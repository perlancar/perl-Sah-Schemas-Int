package Sah::Schema::byte;

use strict;

# AUTHORITY
# DATE
# DIST
# VERSION

our $schema = ['uint8', {
    summary => 'Same as uint8',
    links => [
        {url=>'pm:Sah::Schema::uint8'},
    ],
    links => [
    ],
}];

1;
# ABSTRACT:
